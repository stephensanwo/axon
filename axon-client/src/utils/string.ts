// import pako from 'pako';

// // Function to compress a string
// const compressString = (input: string): string => {
//   const compressed = pako.deflate(input, { to: 'string' });
//   const encoded = btoa(unescape(encodeURIComponent(compressed))); // Encode the compressed data to base64
//   return encoded;
// };
